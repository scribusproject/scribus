INCLUDE_DIRECTORIES(
"${CMAKE_SOURCE_DIR}/scribus"
)

FILE( GLOB GIMP_STYLE_PALETTES *.gpl )
FILE( GLOB XML_PALLETES *.xml )

INSTALL(FILES ${GIMP_STYLE_PALETTES} ${XML_PALLETES}  DESTINATION ${SHAREDIR}swatches )


FILE( GLOB SWATCHES_RESENE Resene/*.xml)
INSTALL(FILES ${SWATCHES_RESENE} DESTINATION ${SHAREDIR}swatches/Resene)
